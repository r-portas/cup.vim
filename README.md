Language support for CUP

Forked from [vim-scripts/cup.vim](https://github.com/vim-scripts/cup.vim)

Can be install by adding the following line (if using vim-plug)

```vim
Plug 'r-portas/cup.vim'
```
